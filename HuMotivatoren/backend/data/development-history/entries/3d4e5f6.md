# test: add integration tests for motivate route

**Hash:** 3d4e5f6  
**Date:** 2026-04-09  
**Author:** alex

## Summary

Added a dedicated integration test suite that exercises the `/api/motivate`
endpoint end-to-end, covering happy path and error scenarios without mocking
the HTTP layer.

## Changes

- Created `tests/backend/motivate.test.ts` using Vitest and `supertest`
- Added `tests/package.json` and `tests/vitest.config.ts`
- Covered scenarios: valid request, missing task field, invalid personality,
  server error simulation
- Added `test:integration` script to root `package.json`

## Notes

Integration tests run against a real in-process Express app to avoid port
conflicts in CI. The LLM service is stubbed at the module level via
`vi.mock`.
