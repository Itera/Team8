# chore: add GitHub Actions CI pipeline

**Hash:** 7a8b9c0  
**Date:** 2026-04-11  
**Author:** sara  

## Summary

Configured a GitHub Actions workflow that runs on every pull request and push
to `main`. The pipeline installs dependencies, builds both packages, and
executes the full test suite.

## Changes

- Created `.github/workflows/ci.yml`
- Steps: checkout, setup Node 20, install root deps, install backend deps,
  install frontend deps, build backend, build frontend, run backend tests,
  run frontend tests, run integration tests
- Added workflow badge to `README.md`

## Notes

The workflow uses `npm ci` for reproducible installs. Node version is pinned
to 20 via the `actions/setup-node` matrix to avoid drift.
